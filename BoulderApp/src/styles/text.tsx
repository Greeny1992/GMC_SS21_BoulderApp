import { StyleSheet } from 'react-native'

const TextStyle = StyleSheet.create({
    title: {
        // flex:4,
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 10,
        marginBottom:10,
    },
    subTitle:{
        fontSize:18,
        fontWeight:'200',
        marginTop: 80
        // marginTop: 10,
        // marginBottom:10,
    }
})

export default TextStyle