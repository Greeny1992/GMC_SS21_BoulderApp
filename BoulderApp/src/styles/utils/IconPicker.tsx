import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    picker:{
        position: 'absolute',
        top: 200,
        zIndex: 10,
        backgroundColor: '#fff'
    }
  

}
);
export default styles;
