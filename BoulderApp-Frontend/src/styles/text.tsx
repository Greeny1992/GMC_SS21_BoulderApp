import { StyleSheet } from 'react-native'

const TextStyle = StyleSheet.create({
    title: {
        flex:4,
        fontSize: 35,
        fontWeight: 400,
        marginTop: 10,
        marginBottom:10,
    },
    subTitle:{
        fontSize:28,
        fontWeight:400,
        marginTop: 10,
        marginBottom:10,
    }
})

export default TextStyle